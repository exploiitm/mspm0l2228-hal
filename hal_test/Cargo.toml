[package]
name = "hal_test"
version = "0.1.0"
edition = "2024"

[dependencies]
cortex-m = { version = "0.7.7", features = ["critical-section-single-core"] }
cortex-m-rt = "0.7.5"
cortex-m-semihosting = "0.5.0"
embedded-hal = "1.0.0"
mspm0l2228-hal = { version = "0.1.0", path = "../hal" }
panic-halt = "1.0.0"
